# Python dependencies for Z21 file reader/writer

# Core dependencies
# struct, pathlib, json, re, tempfile, zipfile, sqlite3, uuid, subprocess,
# platform, os, xml.etree.ElementTree, dataclasses, typing, argparse, 
# collections, hashlib, shutil, tkinter are built-in, no need to install

# Required for GUI application
customtkinter>=5.0.0
Pillow>=9.0.0  # PIL - Required for image processing in GUI

# Optional: For better CLI experience
click>=8.0.0

# Optional: For testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional: For hex dump formatting
hexdump>=3.3

# Optional: For OCR (Scan for Details feature)
pytesseract>=0.3.10
pdf2image>=1.16.3

# Optional: For macOS AirDrop sharing (macOS only)
pyobjc-framework-AppKit>=9.0.0  # macOS only - Required for AirDrop sharing feature
